# Local development (SQLite file)
DATABASE_URL="file:./db/custom.db"

# ── OR ── for production on Vercel (Turso / libSQL) ──
# DATABASE_URL="libsql://your-db.turso.io"
# DATABASE_AUTH_TOKEN="your-turso-token"
